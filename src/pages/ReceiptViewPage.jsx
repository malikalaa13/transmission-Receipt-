import { useEffect, useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import { Download, Edit3, Printer } from 'lucide-react';
import ReceiptRenderer from '../components/receipt/ReceiptRenderer';
import { getReceipt, getSettings } from '../services/dataService';
export default function ReceiptViewPage(){const {id}=useParams();const [receipt,setReceipt]=useState(null),[settings,setSettings]=useState(null);useEffect(()=>{Promise.all([getReceipt(id),getSettings()]).then(([r,s])=>{setReceipt(r);setSettings(s)})},[id]);if(!receipt||!settings)return <div className="p-10 text-center">Loading…</div>;const pdf=async()=>{const res=await fetch(`${import.meta.env.VITE_PDF_API_URL||'http://localhost:8787'}/api/pdf`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({receipt,settings})});if(!res.ok)return window.print();const blob=await res.blob();const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`receipt-${receipt.receipt_no}.pdf`;a.click()};return <div><div className="mb-4 flex items-center justify-between print:hidden"><div><Link to="/" className="text-sm font-bold text-slate-500">← Receipts</Link><h1 className="page-title">Receipt #{receipt.receipt_no}</h1></div><div className="flex gap-2"><Link className="btn-secondary" to={`/receipts/${receipt.id}?edit=1`}><Edit3 size={16}/>Edit</Link><button className="btn-secondary" onClick={()=>window.print()}><Printer size={16}/>Print</button><button className="btn-primary" onClick={pdf}><Download size={16}/>Download PDF</button></div></div><div className="receipt-stage"><ReceiptRenderer receipt={receipt} settings={settings}/></div></div>}
